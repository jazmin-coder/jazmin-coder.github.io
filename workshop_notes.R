#Workshop Notes

#I'm adding a comment to practice committing changes using Git.

#Local and remote repositories are in sync. 

#To update files in remote repository, follow these commands at the terminal each time:
#Save the file locally.
#Run: git add .
#Run: git commit -m "message"
#Run: git push